// By mainitainng a hashtable and check if it already containes the node

// By having a FLAG on each node and set it when we traverse

// Have 2 pointers, move p1 in steps of 1, p2 in steps of 2, if they meet then loop